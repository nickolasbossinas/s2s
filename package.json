{
  "name": "s2s",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm -w packages/demo run dev",
    "build": "npm -w packages/core run build && npm -w packages/demo run build",
    "build:core": "npm -w packages/core run build",
    "typecheck": "npm -w packages/core run typecheck && npm -w packages/demo run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
